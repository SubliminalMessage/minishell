#include <minishell.h>

int main(void)
{
    //char cwd_buffer[1000];
    //char *pwd = getcwd(cwd_buffer, 1000);
    //printf("%s➜ %s %s%s: %s%s\n", GREEN, CYAN, pwd, YELLOW, RESET, "rm -rf test/");

    prompt();
    printf("\n\n");
}